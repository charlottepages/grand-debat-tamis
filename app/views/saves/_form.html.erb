<%= form_with(model: safe, local: true) do |form| %>
  <% if safe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(safe.errors.count, "error") %> prohibited this safe from being saved:</h2>

      <ul>
      <% safe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
